# PROJECT_NOTES — DeepSim Kaggriculture — الوضع الحالي 2026-09-02

> نقطة الحفظ الحالية `bf832c0` على `fix-all-issues` من `bc7f5c1` الأساس `108631` — `mean 111,453` `32 seed` `best 135,725 worst 85,730` `vs pass` (ثابت).

## 0) مشاكل لم تُحل بعد — في البداية (أولوية)

- **حركة مهدورة 69.9%** (`moves 3,826` `PASS 371` `src/planner.py:530`) — `WATER 1,073` `HARVEST 321` `PLANT 222` لا تكفي لتغطية `60` مربع `10×10`، كل `move` بلا مساهمة من السبعة (`زراعة/سقي/تسميد/حصاد/إطعام/جمع سماد/ملء فراغ`) يضيع `turn`.
- **حشائش `max 6` >2** (`THRESHOLD 15` `src/config.py:109` `src/planner.py:259`) — `weeds max 6` `seed 11` `day14-18` (`weeds 12-17` في التتبع) بدل `2` المطلوب.
- **نباتات بلا سقي `8-9`/مباراة** (`consecutive_unwatered 1→2` `overview.txt:151`) → `WEED` وخسارة `80-900$` بذور.
- **سماد `FERTILIZE 0→54`** (`prio 5→2` `STRAWBERRY` `src/planner.py:310`) — كان `0` والآن `54` لكن `market` لا يزال يبيع `FERTILIZER 43` ولا يخزن.
- **تصفية نهاية الموسم `7 STRAWBERRY` متروكة** (`DROP 10` فقط `day29 hour14` `src/planner.py:268`) — `15 WHEAT +12 MELON` بذور في المخزن `seed 11`.
- **سيولة `NE 1000$ day6 / SW 2000$ day9`** (`LAND_DAYS [6,9,12]` `src/config.py:15`) — لا تشترى في الوقت بسبب `money 52$` `hour1` (`seed 0` `day0 hour1`).

## 1) ما تم حله — التوقيع الجغرافي ✅

- **15 حظيرة مركزية `5/ربع`** `NW (4,2)(3,3)(4,3)(3,4)(4,4) NE (5,2)(5,3)(6,3)(5,4)(6,4) SW (2,5)(3,5)(4,5)(3,6)(4,6)` (`src/config.py:68`) — كلها `1-2` خطوة من `SHED` `1-2` لـ`FEED` بدل `5-6`.
- **`PLAN 10/5` (`COW 10 SHEEP 5`) + `plant 60` (`PLANT_TARGET 60` `src/config.py:101`)** — `75-15=60` مربع صالح، `mean 108631→111,453` (`+2,822`).
- **إثبات:** `replay 104875247` `Crop Dusta` `5` حظائر `day1` نفس التوزيع، `seed 0` `day1 5` لا `13` لكن الجغرافيا مطابقة.

## 2) التجارب المرفوضة — لا تكرر

- `staged hiring 5→10→15` (`HIRE 14` `src/config.py:18`) `112k→85k (-27k)` — مرفوض.
- `WATER 1` فوق `PLANT` `114k→88k (-26k)` — مرفوض.
- `3 zones` `112k→93k` — مرفوض، `2×2` أبقى.
- `HARVEST 1` آخر 3 أيام + `DROP 48` `112k→85k` — مرفوض.

## 3) الحالة على السيرفر

- آخر رفع `55962696` `fix-all-issues` `612.4` (`vs pass 111k`) — `Crop Dusta #1 3070.6` (`replay 104875247` `111,584` `day1 13`).
